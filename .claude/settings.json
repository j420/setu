{
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "pip install -q -r requirements-dev.txt >/dev/null 2>&1 || true; echo 'SETU ready: run `python -m demo.run_demo`, `python -m dashboard.app`, or `python -m pytest`.'"
          }
        ]
      }
    ]
  }
}
